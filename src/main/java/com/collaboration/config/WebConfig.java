package com.collaboration.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class WebConfig {

    /* CORS: see CorsConfigurationSource in SecurityConfig (Spring Security CorsFilter). */
}
